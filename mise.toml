# Project runtime pin for Voila.
# Node 22.23.1 satisfies @earendil-works/pi-coding-agent@0.82.0 engines (node >=22.19.0)
# and enables flag-free TypeScript type stripping (Node >= 22.18).
#
# Activate with `mise install` (if mise is installed) or ensure Node 22.23.1 is on PATH.
[tools]
node = "22.23.1"
