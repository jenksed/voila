interface:
  display_name: "Newfang"
  short_description: "Repo-specific patterns and workflows for newfang"
  default_prompt: "Use the newfang repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true