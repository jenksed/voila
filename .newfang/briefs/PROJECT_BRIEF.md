<!-- GENERATED by NewFang from .newfang/project.json — do not edit by hand. -->

# newfang — Project Brief

_Generated by NewFang from canonical state. Non-authoritative: `.newfang/project.json` is the source of truth._

- **Phase**: build · **Health**: green · **Revision**: 27
- **Focus**: NF-2 — Build planning-document intake and repository orientation

## Next justified action

Build claims, verification receipts, and the protected completion transition (NF-3).

**Why now:** Intake, orientation, and the Steward context loop now work end to end, so the next unproven promise is evidence-before-completion: NF-1 has been implemented and tested for two packets but still cannot be marked complete because no protected completion transition exists. NF-3 closes that gap and unblocks NF-4 and the self-hosting gate.

## Key accepted decisions

- `DEC-1` Extensions-first architecture — Ship NewFang as project-local Pi extensions first; package second; SDK/RPC held in reserve.
- `DEC-2` Canonical repository state in .newfang/ — project.json is authoritative; events.jsonl is append-only history; PROJECT_STATUS.md is generated.
- `DEC-3` Thin Pi adapter, modular src/ — .pi/extensions/newfang.ts is a thin adapter; domain/state/tools/ui live in src/ and are Pi-free.
- `DEC-4` Roles as skills before runtime agents — Permanent roles are product concepts/skills/prompts in the MVP, not runtime agents.
- `DEC-5` Sandboxing deferred and optional — Sandboxing is optional and off by default; not an MVP prerequisite.
- `DEC-6` Focus is distinct from lifecycle status — focusWorkItemId names the item receiving attention; it is not a status and does not imply in_progress.
- `DEC-7` The language model performs interpretation; NewFang owns preservation, schema... — The language model performs interpretation; NewFang owns preservation, schemas, lifecycle, review, application, persistence, provenance, and resumability.
- `DEC-8` Model-generated interpretations are never applied to canonical project truth ... — Model-generated interpretations are never applied to canonical project truth silently; the user reviews an understanding check first.
- `DEC-9` Schema version 3 is introduced with an explicit 2 -> 3 migration (inspect, --... — Schema version 3 is introduced with an explicit 2 -> 3 migration (inspect, --apply, backup, atomic replace).

## Open assumptions

- `ASM-1` (medium) The Pi extension/TUI surfaces NewFang uses are stable at the pinned 0.82.0.
- `ASM-2` (high) A single primary macOS/Apple Silicon machine is the initial target.
- `ASM-3` (high) Repository-local orientation is sufficient for this phase; no external research is required.

## Open risks

- `RSK-2` (low/high) Single-writer assumption for .newfang/ could race under concurrent writers.
- `RSK-3` (medium/low) Terminal UI overload as console modules grow.
- `RSK-4` (medium/medium) Scope creep into project-management ceremony.
- `RSK-5` (medium/medium) Interpretation of planning documents is nondeterministic, so the understanding check and human acceptance are the only guarantees that canonical truth is correct.

## Work in flight

- `NF-1` (in progress) Complete the minimal project-operations layer
- `NF-2` (ready, high) Build planning-document intake and repository orientation

## Source intakes

- `INT-1` docs/plans/PHASE_3_INTAKE_BRIEF.md (file: `docs/plans/PHASE_3_INTAKE_BRIEF.md`, sha `d47fb7640215…`)
